[package]
name = "vision"
version = "0.1.0"
authors = ["romain <romain.assie@supinfo.com>"]

[dependencies]
serde = "1.0"
serde_derive = "1.0"
serde_json = "1.0"  # just for the example, not required in general
handlebars = "0.32.*"
rouille = "2.1.0"

